package shaders
//kage:unit pixels

var Threshold float

func Fragment(_ vec4, local vec2, _ vec4) vec4 {
    v := local.x / imageSrc0Size().x

    if v > Threshold {
        return imageSrc0At(local)
    }

    return vec4(0)

}